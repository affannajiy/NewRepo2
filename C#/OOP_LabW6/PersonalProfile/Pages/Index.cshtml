@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<!--Home Page-->
<!--Page Content-->
<div class="text-center">
    <h1 class="display-4">Welcome, Hi I'm 'Affan!</h1>
    <h1 class="display-6">Nice to meet you.</h1>
</div>

<!--Clock-->
<div class="text-center" id="clock">
    <h2>Current Time</h2>
    <!--Script: Java stuff-->
    <div style="font-size: 20px;">
        <script>
            function updateTime() {
                const now = new Date();
                const hours = now.getHours().toString().padStart(2, '0');
                const minutes = now.getMinutes().toString().padStart(2, '0');
                const seconds = now.getSeconds().toString().padStart(2, '0');

                // Format the time as HH:MM:SS
                const currentTime = `${hours}:${minutes}:${seconds}`;

                // Display the time inside the clock div
                document.getElementById('clock').textContent = currentTime;
            }

            // Update the time every second
            setInterval(updateTime, 1000);

            // Call updateTime initially to show the time immediately on load
            updateTime();
        </script>
    </div>
</div>

<div style="text-align: center">
    <img src="~/NICHOLAS &TEAM.jpeg" width="400" height="400" alt="Nicholas">
</div>